import ProfileContent from '../../components/profile/ProfileContent';

/**
 * AdminProfile - Profile page for admin users
 * Uses the reusable ProfileContent component
 */
const AdminProfile = () => {
    return <ProfileContent />;
};

export default AdminProfile;
